let newOutputDiv = document.getElementById("newOutput");
let counter = 1,
    target = 5;

while (counter <= 5) {
    newOutputDiv.innerHTML = newOutputDiv.innerHTML + counter;
    counter = counter + 1; //counter==;
}